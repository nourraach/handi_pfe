# Core Service

Temporary compatibility service.

The current backend in `handi_talents-main_abir` acts as this service while routes are extracted progressively.

Gateway fallback:

- all `/api/*` routes except extracted services route here first.

